<Window x:Class="TestRxp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestRxp"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:TestRxp.ViewModel"
        Title="MainWindow" Height="100" Width="200">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <StackPanel>
        <TextBlock Text="{Binding CurrentTime1.Value}"/>
        <TextBlock Text="{Binding CurrentTime2.Value}"/>
        <TextBlock Text="{Binding CurrentTime3}"/>
    </StackPanel>
</Window>
